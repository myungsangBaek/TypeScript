// JS 문자열 선언
// var str = "hello";

// TS 문자열 선언
const str: string = "hello";

// TS 숫자 선언
const num: number = 10;

// TS 배열 선언
const arr: Array<number> = [1, 2, 3];
const dododot: Array<string> = ["Lil", "Stark", "Robert", "Han"];
const items: number[] = [1, 2, 3];

// TS 튜플
const address1: [string, number] = ["gangnam", 123];

// TS 객체
const obj: object = {};
const person: object = {
  name: "lil",
  age: 100,
};

const person1: { name: string; age: number } = {
  name: "han",
  age: 300,
};

// TS 진위값
const show: boolean = true;
